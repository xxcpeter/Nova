fn first(xs: vec<int>) : int {
    return vec_get(xs, 0);
}

fn main() : void {
    let xs : vec<int> = vec_new();
    vec_push(xs, 10);
    vec_push(xs, 20);

    print_int(first(xs));
    return;
}
