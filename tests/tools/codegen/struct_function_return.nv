struct Point {
    x: int;
    y: int;
}

fn make_point(x: int, y: int) : Point {
    return Point { x: x, y: y };
}

fn main() : void {
    let p : Point = make_point(10, 20);
    print_int(p.x);
    print_int(p.y);
    return;
}
