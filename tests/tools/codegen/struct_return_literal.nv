struct Point {
    x: int;
    y: int;
}

fn make_point() : Point {
    return Point { x: 11, y: 22 };
}

fn main() : void {
    let p : Point = make_point();

    print_int(p.x);
    print_int(p.y);
    return;
}
